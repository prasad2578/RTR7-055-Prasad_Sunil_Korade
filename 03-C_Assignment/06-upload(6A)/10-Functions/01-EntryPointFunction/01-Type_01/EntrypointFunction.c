#include <stdio.h> //stdio.h contain declaration of printf()

// entry point function => main() => no return type (void) and no prameters (void), hence no command - line argument

void main(void)
{
          //code
          printf("Hellow World !!!\n");
}
